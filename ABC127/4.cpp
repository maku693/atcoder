#include <iostream>
#include <limits>
#include <vector>

int main(void) {
  int n, m;
  std::cin >> n >> m;

  std::vector<int> a(n);
  int a_min = std::numeric_limits<int>::max();
  int a_max = 0;

  for (int i = 0; i < n; i++) {
    std::cin >> a[i];
  }



  std::cout 

  return 0;
}
